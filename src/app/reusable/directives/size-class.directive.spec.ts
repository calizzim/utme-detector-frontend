import { SizeClassDirective } from './size-class.directive';

describe('SizeClassDirective', () => {
  it('should create an instance', () => {
    const directive = new SizeClassDirective();
    expect(directive).toBeTruthy();
  });
});
