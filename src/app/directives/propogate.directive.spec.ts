import { PropogateDirective } from './propogate.directive';

describe('PropogateDirective', () => {
  it('should create an instance', () => {
    const directive = new PropogateDirective();
    expect(directive).toBeTruthy();
  });
});
